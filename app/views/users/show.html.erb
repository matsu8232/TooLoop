<div class="cover-user-show">
    <div class="container d-flex flex-column align-items-center">
        <div class="row mt-5">
            <% if user_signed_in? && @user == current_user %>
                <h1 class="font-weight-normal">マイページ</h1>
            <% else %>
                <h1 class="font-weight-normal"><%= @user.name %>さんのページ</h1>
            <% end %>
        </div>
        <% if user_signed_in? && @user == current_user %>
            <p class="d-flex align-self-end">
                <%= link_to "プロフィール編集", edit_user_path(@user), class: "btn btn-success" %>
            </p>
        <% end %>
        <div class="d-flex w-100">
            <% if @user.avatar.attached? %>
                <%= image_tag @user.avatar, size: '100x100', class: "mb-3 avatar" %>
            <% else %>
                <%= image_tag "no_avatar.svg", size: '100x100', class: "mb-3 avatar" %>
            <% end %>
            <h4 class="ml-3 mb-3 card w-100 info"><%= @user.info %></h4>
        </div>
        <div class="row mt-5 index-left w-100">
            <% @items.each do |item| %>
                <div class="one-post col-sm-3 mb-3">
                    <%= link_to item_path(item) do %>
                        <% if item.image.attached? %>
                            <%= image_tag item.image.variant(resize_to_limit: [200, 133]) %>
                        <% else %>
                            <%= image_tag 'no_image', size: "200x133" %>
                        <% end %>
                        <p class="caption mb-0"><%= item.name %></p>
                    <% end %>
                </div>
            <% end %>
        </div>
        <%= paginate @items %>
        
        <% if user_signed_in? && @user == current_user %>
            <h2 class="mt-4">予約した備品</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th>備品名</th>
                        <th>期間</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <% @future_reservations.each do |reservation| %>
                        <tr>
                            <td><%= link_to reservation.item.name, item_path(reservation.item) %></td>
                            <td><%= reservation.start_date %> 〜 <%= reservation.end_date %></td>
                            <td><%= link_to '詳細', item_reservation_path(reservation.item, reservation), class: 'btn btn-sm btn-outline-primary' %></td>
                        </tr>
                    <% end %>
                    <% if @future_reservations.empty? %>
                        <tr>
                            <td colspan="3">現在、予約している備品はありません。</td>
                        </tr>
                    <% end %>
                </tbody>
            </table>

            <h2 class="mt-4">過去の予約</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th>備品名</th>
                        <th>期間</th>
                    </tr>
                </thead>
                <tbody>
                    <% @past_reservations.each do |reservation| %>
                        <tr>
                            <td><%= link_to reservation.item.name, item_path(reservation.item) %></td>
                            <td><%= reservation.start_date %> 〜 <%= reservation.end_date %></td>
                        </tr>
                    <% end %>
                    <% if @past_reservations.empty? %>
                        <tr>
                            <td colspan="2">過去の予約はありません。</td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        <% end %>
    </div>
</div>